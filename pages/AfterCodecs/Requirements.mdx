import ProductPage, { BackToProductLink } from "../../components/ProductPage";
import { Requirements } from "../../components/content/AfterCodecs/Overview";
import { meta } from './index.mdx';

export default props => (
  <ProductPage
    meta={meta}
    landerImage={require("assets/images/_AC/AC_product_background.jpg?trace")}
    {...props}
  />
);

<BackToProductLink {...meta} product="AfterCodecs" title="Technical Specifications" />
<Requirements mainColor={meta.mainColor} / >